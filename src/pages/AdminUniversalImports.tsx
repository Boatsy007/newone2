import { useEffect,useMemo,useState,type CSSProperties } from 'react'
import { ArrowLeft,History,RefreshCw,UploadCloud } from 'lucide-react'
import { Link,useNavigate } from 'react-router-dom'
import { getKey,setKey } from '../lib/admin'
type Kind='ladder'|'results'|'fixtures'|'goalKickers'|'club'|'league'|'players'|'unknown'
type Item={id:string;file:File;url:string;status:'ready'|'classifying'|'classified'|'failed';kind:Kind;confidence:number;reason:string;error?:string}
type HistoryRow={id:string;kind:string;status:string;title:string;detail:string;confidence:number|null;createdAt:string;committedAt:string|null;revert:string}
const input:CSSProperties={width:'100%',padding:'10px 11px',border:'1px solid #dce3eb',borderRadius:10,font:'inherit',boxSizing:'border-box'}
const button=(dark=false):CSSProperties=>({border:0,borderRadius:999,padding:'11px 16px',background:dark?'#050505':'#42b8ff',color:dark?'#fff':'#050505',fontWeight:900,cursor:'pointer',display:'inline-flex',alignItems:'center',gap:7,justifyContent:'center'})
const routeFor=(k:Kind)=>k==='ladder'?'/admin':k==='results'||k==='fixtures'?'/admin/match-images':k==='goalKickers'?'/admin/goal-kicker-images':k==='club'||k==='league'||k==='players'?'/admin/profile-images':'/admin'
export default function AdminUniversalImports(){const nav=useNavigate();const[authed,setAuthed]=useState(!!getKey());const[key,setLocalKey]=useState('');const[items,setItems]=useState<Item[]>([]);const[history,setHistory]=useState<HistoryRow[]>([]);const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');const loadHistory=()=>fetch('/admin/universal-imports/history',{headers:{authorization:`Bearer ${getKey()}`}}).then(r=>r.ok?r.json():Promise.reject(new Error(`HTTP ${r.status}`))).then((r:{data:HistoryRow[]})=>setHistory(r.data)).catch(()=>setHistory([]));useEffect(()=>{if(authed)loadHistory()},[authed]);const add=(files:FileList)=>setItems(p=>[...p,...Array.from(files).filter(f=>f.type.startsWith('image/')).map(file=>({id:`${Date.now()}-${Math.random()}`,file,url:URL.createObjectURL(file),status:'ready' as const,kind:'unknown' as const,confidence:0,reason:''}))]);const classify=async()=>{setBusy(true);for(const item of items.filter(i=>i.status==='ready'||i.status==='failed')){setItems(p=>p.map(x=>x.id===item.id?{...x,status:'classifying',error:undefined}:x));try{const image=await dataUrl(item.file);const r=await fetch('/admin/universal-imports/classify',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${getKey()}`},body:JSON.stringify({image})});const j=await r.json();if(!r.ok)throw new Error(j.error??`HTTP ${r.status}`);setItems(p=>p.map(x=>x.id===item.id?{...x,status:'classified',kind:j.data.kind,confidence:j.data.confidence,reason:j.data.reason}:x))}catch(e){setItems(p=>p.map(x=>x.id===item.id?{...x,status:'failed',error:e instanceof Error?e.message:String(e)}:x))}}setBusy(false);setMsg('Classification complete. Open each image in its specialised review workflow; no data has been committed.')};const counts=useMemo(()=>items.reduce<Record<string,number>>((a,x)=>(a[x.kind]=(a[x.kind]??0)+1,a),{}),[items]);if(!authed)return <div style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#050505'}}><div style={{background:'#fff',padding:24,borderRadius:18,width:'min(92vw,420px)'}}><h1>Universal imports</h1><input style={input} type="password" value={key} onChange={e=>setLocalKey(e.target.value)}/><button style={{...button(),width:'100%',marginTop:12}} onClick={()=>{if(key.trim()){setKey(key.trim());setAuthed(true)}}}>Open</button></div></div>;return <main style={{minHeight:'100vh',background:'#eef2f6',padding:24,fontFamily:'Inter,system-ui,sans-serif'}}><div style={{maxWidth:1400,margin:'0 auto',display:'grid',gap:18}}><header style={{background:'#050505',color:'#fff',borderRadius:20,padding:28}}><Link to="/admin" style={{color:'#42b8ff',textDecoration:'none',display:'inline-flex',gap:7,alignItems:'center'}}><ArrowLeft size={16}/>Back to Control Centre</Link><h1 style={{fontSize:'clamp(42px,7vw,84px)',margin:'12px 0 6px',textTransform:'uppercase'}}>Universal image imports</h1><p>Upload a mixed batch, classify it automatically, then send each screenshot to its protected review-and-approval workflow.</p></header><label style={{minHeight:190,border:'2px dashed #92cfee',borderRadius:18,background:'#f7fcff',display:'grid',placeItems:'center',textAlign:'center',padding:24,cursor:'pointer'}}><div><UploadCloud size={44}/><h2>Choose mixed screenshots</h2><span style={button()}>Select images</span></div><input hidden type="file" accept="image/*" multiple onChange={e=>{if(e.target.files)add(e.target.files);e.currentTarget.value=''}}/></label>{items.length>0&&<><div style={{display:'flex',gap:8,flexWrap:'wrap'}}>{Object.entries(counts).map(([k,n])=><span key={k} style={{background:'#fff',padding:'8px 12px',borderRadius:999,border:'1px solid #dce3eb'}}>{k}: {n}</span>)}</div><section style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:12}}>{items.map(item=><article key={item.id} style={{background:'#fff',borderRadius:16,overflow:'hidden',border:'1px solid #dce3eb'}}><img src={item.url} style={{width:'100%',height:165,objectFit:'cover'}}/><div style={{padding:14,display:'grid',gap:8}}><strong>{item.file.name}</strong><select style={input} value={item.kind} onChange={e=>setItems(p=>p.map(x=>x.id===item.id?{...x,kind:e.target.value as Kind,status:'classified'}:x))}>{['unknown','ladder','results','fixtures','goalKickers','club','league','players'].map(k=><option key={k} value={k}>{k}</option>)}</select><small>{item.status}{item.status==='classified'?` · ${Math.round(item.confidence*100)}% · ${item.reason}`:''}{item.error?` · ${item.error}`:''}</small><div style={{display:'flex',gap:8}}><button style={button(true)} disabled={busy} onClick={()=>setItems(p=>p.map(x=>x.id===item.id?{...x,status:'ready',error:undefined}:x))}><RefreshCw size={14}/>Retry</button><button style={button()} disabled={item.kind==='unknown'} onClick={()=>nav(routeFor(item.kind))}>Open importer</button></div></div></article>)}</section><button style={button()} disabled={busy} onClick={classify}>{busy?'Classifying…':'Automatically classify batch'}</button></>}{msg&&<div style={{background:'#fff8dc',padding:14,borderRadius:12}}>{msg}</div>}<section style={{background:'#fff',borderRadius:18,padding:18}}><header style={{display:'flex',alignItems:'center',justifyContent:'space-between',gap:12}}><div><h2 style={{margin:0,display:'flex',alignItems:'center',gap:8}}><History size={20}/>Import history</h2><p>Committed imports and OCR previews, with audit-safe correction guidance.</p></div><button style={button(true)} onClick={loadHistory}><RefreshCw size={14}/>Refresh</button></header><div style={{overflow:'auto'}}><table style={{width:'100%',borderCollapse:'collapse',minWidth:900}}><thead><tr>{['When','Type','Status','Import','Details','Confidence','Correction / revert path'].map(h=><th key={h} style={{padding:10,textAlign:'left',borderBottom:'1px solid #e5e7eb'}}>{h}</th>)}</tr></thead><tbody>{history.map(r=><tr key={`${r.kind}-${r.id}`}><td style={{padding:10,borderBottom:'1px solid #eef2f6'}}>{new Date(r.createdAt).toLocaleString('en-AU')}</td><td>{r.kind}</td><td>{r.status}</td><td>{r.title}</td><td>{r.detail}</td><td>{r.confidence==null?'—':`${Math.round(r.confidence*100)}%`}</td><td>{r.revert}</td></tr>)}</tbody></table></div></section></div></main>}
function dataUrl(file:File){return new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result));r.onerror=()=>reject(r.error);r.readAsDataURL(file)})}

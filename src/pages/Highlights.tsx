import { useEffect, useMemo, useState, type FormEvent } from 'react'
import { ArrowRight, Award, Check, Clock3, ExternalLink, Play, ShieldCheck, Trophy, Upload, Vote } from 'lucide-react'
import Nav from '../components/layout/Nav'
import Footer from '../components/layout/Footer'
import { useSeo } from '../lib/seo'

type Category = 'goal' | 'mark' | 'play' | 'performance'
type Highlight = {
  id: string; category: Category; playerName: string; clubId?: string | null; clubName: string
  leagueId?: string | null; leagueName?: string | null; matchDate?: string | null; roundLabel?: string | null
  videoUrl: string; thumbnailUrl?: string | null; description?: string | null; weekKey: string
  votingOpensAt?: string | null; votingClosesAt?: string | null; votingOpen: boolean; winner: boolean; votes: number
}

const categories: Array<{ id: Category; title: string; eyebrow: string; description: string; icon: typeof Trophy }> = [
  { id: 'goal', title: 'Goal of the week', eyebrow: 'The impossible finish', description: 'The best snaps, runs, set shots and match-winning goals.', icon: Trophy },
  { id: 'mark', title: 'Mark of the week', eyebrow: 'The biggest grabs', description: 'Contested marks, high flyers and courageous intercepts.', icon: Award },
  { id: 'play', title: 'Play of the week', eyebrow: 'The moment that changed it', description: 'Team football, pressure acts, chases and brilliant passages.', icon: Play },
  { id: 'performance', title: 'Performance of the week', eyebrow: 'The standout player', description: 'The complete individual performance deserving a national spotlight.', icon: ShieldCheck },
]

export default function Highlights() {
  const [rows, setRows] = useState<Highlight[]>([])
  const [archive, setArchive] = useState<Highlight[]>([])
  const [weekKey, setWeekKey] = useState('')
  const [loading, setLoading] = useState(true)
  const [message, setMessage] = useState('')
  const [showSubmit, setShowSubmit] = useState(false)
  const load = () => {
    setLoading(true)
    Promise.all([
      fetch('/api/highlights').then(r => r.ok ? r.json() : Promise.reject(new Error('Unable to load highlights'))),
      fetch('/api/highlights/archive').then(r => r.ok ? r.json() : Promise.reject(new Error('Unable to load archive'))),
    ]).then(([current, past]) => { setRows(current.data ?? []); setWeekKey(current.meta?.weekKey ?? ''); setArchive(past.data ?? []) }).catch(e => setMessage(e.message)).finally(() => setLoading(false))
  }
  useEffect(load, [])

  useSeo({ title: 'Weekly Awards & Community Football Highlights | PlayFooty', description: "Vote for Australia's best community football goals, marks, plays and performances each week.", path: '/highlights' })

  const groups = useMemo(() => Object.fromEntries(categories.map(category => [category.id, rows.filter(row => row.category === category.id)])) as Record<Category, Highlight[]>, [rows])
  const openCount = rows.filter(row => row.votingOpen).length

  const vote = async (row: Highlight) => {
    setMessage('')
    const response = await fetch(`/api/highlights/${row.id}/vote`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: '{}' })
    const json = await response.json().catch(() => ({}))
    if (!response.ok) return setMessage(json.error ?? 'Vote could not be recorded')
    setRows(current => current.map(item => item.id === row.id ? { ...item, votes: json.data.votes } : item))
    setMessage(`Your vote for ${row.playerName} has been recorded.`)
  }

  return <div className="pf-awards-page"><Nav /><main id="main-content">
    <section className="pf-awards-hero"><div className="pf-awards-shell pf-awards-hero-inner">
      <div className="pf-awards-hero-copy"><span className="pf-awards-kicker"><Vote size={16}/> Fan voted. Club driven.</span><h1>PlayFooty<br/><em>weekly awards</em></h1><p>Australia's best community football moments, chosen by the people who live them.</p><div className="pf-awards-actions"><a href="#awards" className="primary">Vote now <ArrowRight size={18}/></a><button onClick={() => setShowSubmit(true)}><Upload size={17}/> Submit a highlight</button></div></div>
      <div className="pf-awards-hero-panel"><span><i/> {openCount ? 'Voting live now' : 'Weekly voting'}</span><strong>{rows.length ? `${rows.length} nominees` : 'Nominees coming soon'}</strong><p>{rows.length ? `Week ${weekKey}. Choose one nominee in each category while voting is open.` : 'Clubs and supporters can submit verified highlights for moderation.'}</p><div><Clock3 size={18}/><span>{openCount ? 'Live vote totals update after every accepted vote.' : 'Voting windows are set by PlayFooty after moderation.'}</span></div></div>
    </div></section>

    {message && <div className="pf-awards-message">{message}</div>}

    <section id="awards" className="pf-awards-shell pf-awards-section"><header className="pf-awards-heading"><div><span>Choose the moment</span><h2>This week's awards</h2></div><p>Every nominee below has been reviewed and approved before publication.</p></header>
      {loading ? <div className="pf-awards-empty">Loading this week's nominees…</div> : <div className="pf-awards-category-stack">{categories.map(category => <CategoryBlock key={category.id} category={category} rows={groups[category.id]} vote={vote}/>)}</div>}
    </section>

    <section className="pf-awards-process"><div className="pf-awards-shell"><header className="pf-awards-heading light"><div><span>How it works</span><h2>From local moment to national winner</h2></div></header><div className="pf-awards-steps"><Step number="01" icon={Upload} title="Submit" text="Supporters and clubs send the video, player, club, league and match details."/><Step number="02" icon={ShieldCheck} title="Moderation" text="PlayFooty checks every submission before it becomes a public nominee."/><Step number="03" icon={Vote} title="Vote" text="One vote per category each week from each supporter or device."/><Step number="04" icon={Trophy} title="Winner" text="The winning moment is selected and preserved in the permanent archive."/></div></div></section>

    <section className="pf-awards-shell pf-awards-archive"><div><span>Hall of fame</span><h2>Every winner.<br/>Every week.<br/><em>Forever.</em></h2><p>Weekly winners remain attached to their player, club, league and original video.</p></div><div className="pf-awards-archive-list">{archive.map(row => <a key={row.id} href={row.videoUrl} target="_blank" rel="noreferrer"><Trophy size={20}/><span><strong>{row.playerName}</strong><small>{row.clubName} · {row.weekKey} · {row.votes} votes</small></span><ExternalLink size={16}/></a>)}{archive.length === 0 && <div className="pf-awards-empty">No winners have been published yet.</div>}</div></section>

    <section className="pf-awards-cta"><div className="pf-awards-shell"><div><span>Captured something special?</span><strong>Put your club's moment in front of Australia.</strong></div><button onClick={() => setShowSubmit(true)}>Submit highlight <ArrowRight size={18}/></button></div></section>
    {showSubmit && <SubmissionModal close={() => setShowSubmit(false)} submitted={text => { setShowSubmit(false); setMessage(text) }}/>} 
  </main><Footer/><Styles/></div>
}

function CategoryBlock({ category, rows, vote }: { category: typeof categories[number]; rows: Highlight[]; vote: (row: Highlight) => void }) {
  const Icon = category.icon
  return <section className="pf-category"><header><span><Icon size={21}/></span><div><small>{category.eyebrow}</small><h3>{category.title}</h3><p>{category.description}</p></div></header>{rows.length ? <div className="pf-nominee-grid">{rows.map(row => <Nominee key={row.id} row={row} vote={vote}/>)}</div> : <div className="pf-awards-empty">No approved nominees in this category yet.</div>}</section>
}

function Nominee({ row, vote }: { row: Highlight; vote: (row: Highlight) => void }) {
  return <article className="pf-nominee"><a href={row.videoUrl} target="_blank" rel="noreferrer" className="pf-nominee-media" style={row.thumbnailUrl ? { backgroundImage: `linear-gradient(rgba(0,0,0,.16),rgba(0,0,0,.38)),url(${row.thumbnailUrl})` } : undefined}><Play size={34} fill="currentColor"/><span>Watch highlight</span></a><div className="pf-nominee-copy">{row.winner && <span className="winner"><Trophy size={13}/> Weekly winner</span>}<h4>{row.playerName}</h4><strong>{row.clubName}</strong><small>{row.leagueName || 'Community football'}{row.roundLabel ? ` · ${row.roundLabel}` : ''}</small>{row.description && <p>{row.description}</p>}<div><b>{row.votes} votes</b><button disabled={!row.votingOpen} onClick={() => vote(row)}>{row.votingOpen ? 'Vote' : 'Voting closed'}</button></div></div></article>
}

function SubmissionModal({ close, submitted }: { close: () => void; submitted: (message: string) => void }) {
  const [busy, setBusy] = useState(false); const [error, setError] = useState('')
  const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setBusy(true); setError(''); const body = Object.fromEntries(new FormData(event.currentTarget).entries()); const response = await fetch('/api/highlights/submissions', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(body) }); const json = await response.json().catch(() => ({})); setBusy(false); if (!response.ok) return setError(json.error ?? 'Submission failed'); submitted('Highlight submitted. PlayFooty will review it before it appears publicly.') }
  return <div className="pf-submit-backdrop" onClick={close}><form className="pf-submit-modal" onClick={e => e.stopPropagation()} onSubmit={submit}><header><div><span>Weekly awards</span><h2>Submit a highlight</h2></div><button type="button" onClick={close}>×</button></header><div className="pf-submit-grid"><label>Category<select name="category" required>{categories.map(c => <option key={c.id} value={c.id}>{c.title}</option>)}</select></label><label>Player name<input name="playerName" required/></label><label>Club name<input name="clubName" required/></label><label>League name<input name="leagueName"/></label><label>Match date<input type="date" name="matchDate"/></label><label>Round<input name="roundLabel" placeholder="Round 8"/></label><label className="wide">Video URL<input type="url" name="videoUrl" required placeholder="YouTube, Vimeo, Facebook or direct video link"/></label><label className="wide">Thumbnail URL<input type="url" name="thumbnailUrl" placeholder="Optional image URL"/></label><label className="wide">Description<textarea name="description" rows={3}/></label><label>Your name<input name="submitterName" required/></label><label>Your email<input type="email" name="submitterEmail" required/></label></div>{error && <p className="pf-submit-error">{error}</p>}<footer><small>Submission does not guarantee publication. Videos must be yours to share or supplied with permission.</small><button disabled={busy}>{busy ? 'Submitting…' : 'Submit for review'}</button></footer></form></div>
}

function Step({ number, icon: Icon, title, text }: { number: string; icon: typeof Trophy; title: string; text: string }) { return <article><span>{number}</span><Icon size={24}/><h3>{title}</h3><p>{text}</p></article> }

function Styles(){return <style>{`.pf-awards-page{min-height:100vh;background:#fff;color:#111318;font-family:Barlow,Inter,Arial,sans-serif}.pf-awards-shell{width:min(1280px,calc(100% - 48px));margin:0 auto}.pf-awards-hero{background:#050505;color:#fff;border-bottom:5px solid #42b8ff}.pf-awards-hero-inner{min-height:560px;display:grid;grid-template-columns:1.15fr .85fr;gap:70px;align-items:center;padding:64px 0}.pf-awards-kicker,.pf-awards-heading span,.pf-awards-archive>div>span,.pf-submit-modal header span{color:#42b8ff;font-size:11px;font-weight:900;letter-spacing:.17em;text-transform:uppercase}.pf-awards-hero h1,.pf-awards-heading h2,.pf-category h3,.pf-nominee h4,.pf-awards-process h3,.pf-awards-archive h2,.pf-awards-hero-panel strong,.pf-awards-cta strong,.pf-submit-modal h2{font-family:'Bebas Neue',Impact,sans-serif;text-transform:uppercase}.pf-awards-hero h1{font-size:clamp(5rem,9vw,9rem);line-height:.78;margin:18px 0}.pf-awards-hero h1 em,.pf-awards-archive h2 em{font-style:normal;color:#42b8ff}.pf-awards-hero-copy>p{font-size:20px;color:#d8dee5}.pf-awards-actions{display:flex;gap:12px;margin-top:28px;flex-wrap:wrap}.pf-awards-actions a,.pf-awards-actions button,.pf-awards-cta button{display:inline-flex;align-items:center;gap:9px;min-height:48px;padding:0 20px;border-radius:7px;text-transform:uppercase;font-size:12px;font-weight:900;cursor:pointer}.pf-awards-actions .primary,.pf-awards-cta button{background:#42b8ff;color:#050505;border:0;text-decoration:none}.pf-awards-actions button{background:transparent;color:#fff;border:1px solid #555}.pf-awards-hero-panel{padding:30px;border:1px solid #333;border-radius:12px;background:#0d0f12}.pf-awards-hero-panel>span{display:flex;gap:9px;align-items:center;font-weight:900;text-transform:uppercase;font-size:11px}.pf-awards-hero-panel i{width:9px;height:9px;background:#42b8ff;border-radius:50%}.pf-awards-hero-panel strong{display:block;font-size:clamp(3rem,5vw,5rem);line-height:.85;margin-top:28px}.pf-awards-hero-panel p{color:#cbd3dc;line-height:1.5}.pf-awards-hero-panel>div{display:flex;gap:10px;color:#42b8ff;border-top:1px solid #333;padding-top:20px}.pf-awards-message{position:sticky;top:76px;z-index:30;background:#e8f7ff;border-bottom:1px solid #b7e4ff;padding:13px;text-align:center;font-weight:850}.pf-awards-section{padding:64px 0}.pf-awards-heading{display:flex;justify-content:space-between;align-items:end;gap:30px;margin-bottom:25px}.pf-awards-heading h2{font-size:clamp(3rem,5.5vw,5.5rem);line-height:.86;margin:6px 0}.pf-awards-heading p{max-width:430px;color:#687385}.pf-awards-category-stack{display:grid;gap:24px}.pf-category{border:1px solid #e0e5ea;border-radius:12px;padding:22px;background:#fff}.pf-category>header{display:flex;gap:15px;align-items:flex-start;margin-bottom:18px}.pf-category>header>span{display:grid;place-items:center;width:44px;height:44px;border-radius:7px;background:#42b8ff}.pf-category small{color:#42b8ff;font-weight:900;text-transform:uppercase;letter-spacing:.13em}.pf-category h3{font-size:38px;margin:4px 0}.pf-category p{margin:0;color:#687385}.pf-nominee-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.pf-nominee{display:grid;grid-template-columns:210px minmax(0,1fr);border:1px solid #e2e7ec;border-radius:10px;overflow:hidden}.pf-nominee-media{min-height:210px;display:grid;place-items:center;align-content:center;gap:10px;background:#101215 center/cover;color:#42b8ff;text-decoration:none}.pf-nominee-media span{color:#fff;font-size:11px;font-weight:900;text-transform:uppercase}.pf-nominee-copy{padding:18px}.pf-nominee-copy .winner{display:inline-flex;align-items:center;gap:6px;background:#42b8ff;border-radius:999px;padding:5px 8px;font-size:10px;font-weight:900;text-transform:uppercase}.pf-nominee h4{font-size:31px;margin:10px 0 2px}.pf-nominee-copy>small{display:block;color:#687385;margin-top:4px}.pf-nominee-copy>p{font-size:13px;margin-top:10px}.pf-nominee-copy>div:last-child{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-top:16px}.pf-nominee-copy button{border:0;border-radius:7px;background:#42b8ff;padding:10px 16px;font-weight:900;text-transform:uppercase}.pf-nominee-copy button:disabled{background:#edf0f3;color:#89929d}.pf-awards-empty{padding:32px;border:1px dashed #cfd7df;border-radius:10px;text-align:center;color:#687385}.pf-awards-process{background:#050505;color:#fff;padding:65px 0}.pf-awards-heading.light h2{color:#fff}.pf-awards-steps{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.pf-awards-steps article{position:relative;padding:24px;border:1px solid #292d33;border-radius:10px;background:#0c0d0f}.pf-awards-steps article>span{position:absolute;right:17px;color:#42b8ff;font-family:'Bebas Neue',Impact,sans-serif;font-size:28px}.pf-awards-steps svg{color:#42b8ff;margin-top:18px}.pf-awards-steps h3{font-size:29px}.pf-awards-steps p{color:#b9c2cc;line-height:1.5}.pf-awards-archive{display:grid;grid-template-columns:1fr 1fr;gap:60px;padding:70px 0}.pf-awards-archive h2{font-size:clamp(4rem,7vw,7rem);line-height:.8;margin:8px 0}.pf-awards-archive p{color:#687385}.pf-awards-archive-list{display:grid;gap:10px;align-content:start}.pf-awards-archive-list>a{display:flex;align-items:center;gap:12px;border:1px solid #e0e5ea;border-radius:9px;padding:14px;color:#111318;text-decoration:none}.pf-awards-archive-list>a>span{flex:1}.pf-awards-archive-list small{display:block;color:#687385;margin-top:3px}.pf-awards-cta{background:#42b8ff}.pf-awards-cta>.pf-awards-shell{min-height:145px;display:flex;justify-content:space-between;align-items:center;gap:20px}.pf-awards-cta span{display:block;font-size:11px;text-transform:uppercase;font-weight:900}.pf-awards-cta strong{font-size:35px}.pf-submit-backdrop{position:fixed;inset:0;z-index:140;background:rgba(0,0,0,.7);display:grid;place-items:center;padding:16px}.pf-submit-modal{width:min(820px,100%);max-height:92vh;overflow:auto;background:#fff;border-radius:14px}.pf-submit-modal header{display:flex;justify-content:space-between;padding:22px;border-bottom:1px solid #e1e6eb}.pf-submit-modal h2{font-size:42px;margin:4px 0}.pf-submit-modal header button{border:0;background:transparent;font-size:32px}.pf-submit-grid{display:grid;grid-template-columns:1fr 1fr;gap:13px;padding:22px}.pf-submit-grid label{display:grid;gap:6px;font-size:12px;font-weight:850}.pf-submit-grid .wide{grid-column:1/-1}.pf-submit-grid input,.pf-submit-grid select,.pf-submit-grid textarea{border:1px solid #dce2e8;border-radius:8px;padding:12px;font:inherit}.pf-submit-modal footer{display:flex;align-items:center;justify-content:space-between;gap:20px;padding:20px 22px;border-top:1px solid #e1e6eb}.pf-submit-modal footer small{color:#687385}.pf-submit-modal footer button{border:0;border-radius:7px;background:#42b8ff;padding:13px 18px;font-weight:900;text-transform:uppercase}.pf-submit-error{margin:0 22px 12px;color:#b42318;font-weight:800}@media(max-width:900px){.pf-awards-hero-inner,.pf-awards-archive{grid-template-columns:1fr}.pf-awards-steps{grid-template-columns:repeat(2,1fr)}.pf-nominee-grid{grid-template-columns:1fr}.pf-awards-hero-inner{gap:30px}.pf-awards-heading{display:block}}@media(max-width:600px){.pf-awards-shell{width:min(100% - 28px,1280px)}.pf-awards-hero-inner{padding:45px 0}.pf-awards-hero h1{font-size:4.6rem}.pf-awards-steps{grid-template-columns:1fr}.pf-nominee{grid-template-columns:1fr}.pf-nominee-media{min-height:180px}.pf-category{padding:15px}.pf-awards-archive{gap:25px}.pf-awards-cta>.pf-awards-shell{align-items:flex-start;flex-direction:column;justify-content:center;padding:25px 0}.pf-submit-grid{grid-template-columns:1fr}.pf-submit-grid .wide{grid-column:auto}.pf-submit-modal footer{align-items:stretch;flex-direction:column}}`}</style>}

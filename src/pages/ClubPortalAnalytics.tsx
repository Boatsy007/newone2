import { useEffect, useMemo, useState } from 'react'
import { ArrowLeft, BarChart3, Eye, MousePointerClick, Smartphone, TrendingUp, Users } from 'lucide-react'
import { Link, useLocation } from 'react-router-dom'
import Nav from '../components/layout/Nav'
import Footer from '../components/layout/Footer'

type Session={access_token:string}
type Analytics={club:{id:string;name:string};period:string;profileViews:number;viewsLast7Days:number;uniqueVisitors:number;growthPct:number;sponsorImpressions:number;sponsorClicks:number;sponsorClickThroughRate:number;devices:Array<{device:string;views:number}>;dailyViews:Array<{date:string;views:number}>}
const SESSION_KEY='playfooty.clubPortal.session.v1'
function session():Session|null{try{const raw=localStorage.getItem(SESSION_KEY);return raw?JSON.parse(raw):null}catch{return null}}

export default function ClubPortalAnalytics(){
 const{pathname}=useLocation();const clubId=decodeURIComponent(pathname.match(/^\/club-portal\/([^/]+)\/analytics/)?.[1]||'');const[current]=useState(()=>session());const[data,setData]=useState<Analytics|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('')
 const headers=useMemo(()=>current?{authorization:`Bearer ${current.access_token}`}:{authorization:''},[current])
 useEffect(()=>{if(!current||!clubId){setError('Sign in through the Club Portal to view analytics.');setLoading(false);return}let active=true;fetch(`/api/analytics/club-portal/clubs/${encodeURIComponent(clubId)}`,{headers,cache:'no-store'}).then(async response=>{const payload=await response.json();if(!response.ok)throw new Error(payload.error||'Unable to load analytics');if(active)setData(payload.data)}).catch(reason=>{if(active)setError(reason instanceof Error?reason.message:'Unable to load analytics')}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[clubId,current,headers])
 const max=Math.max(1,...(data?.dailyViews.map(row=>row.views)||[1]))
 return <><Nav/><main className="ca"><header><Link to={`/club-portal/${clubId}`}><ArrowLeft size={17}/>Club portal</Link><span>CLUB PERFORMANCE</span><h1>Analytics</h1><p>Profile engagement and sponsor exposure from the last 30 days.</p></header>{loading?<section className="ca-state">Loading analytics…</section>:error?<section className="ca-state error">{error}</section>:data&&<><section className="ca-grid"><Card icon={<Eye/>} label="Profile views" value={data.profileViews}/><Card icon={<Users/>} label="Unique visitors" value={data.uniqueVisitors}/><Card icon={<TrendingUp/>} label="30-day growth" value={`${data.growthPct>0?'+':''}${data.growthPct}%`}/><Card icon={<BarChart3/>} label="Sponsor impressions" value={data.sponsorImpressions}/><Card icon={<MousePointerClick/>} label="Sponsor clicks" value={data.sponsorClicks}/><Card icon={<Smartphone/>} label="Sponsor CTR" value={`${data.sponsorClickThroughRate}%`}/></section><section className="ca-panels"><article><h2>Daily profile views</h2><div className="ca-chart">{data.dailyViews.length?data.dailyViews.map(row=><div key={row.date} title={`${row.date}: ${row.views}`}><i style={{height:`${Math.max(6,(row.views/max)*100)}%`}}/><span>{new Date(row.date).getDate()}</span></div>):<p>No profile views recorded yet.</p>}</div></article><article><h2>Devices</h2>{data.devices.length?data.devices.map(row=><div className="ca-device" key={row.device}><span>{row.device.toLowerCase()}</span><b>{row.views}</b></div>):<p>No device data recorded yet.</p>}<small>Aggregated only. PlayFooty does not show clubs the identity of individual visitors.</small></article></section></>}</main><Footer/><style>{styles}</style></>
}
function Card({icon,label,value}:{icon:React.ReactNode;label:string;value:string|number}){return <article className="ca-card">{icon}<span>{label}</span><strong>{value}</strong></article>}
const styles=`.ca{min-height:80vh;background:#eef3f7;padding:28px clamp(14px,4vw,42px) 110px;color:#111318}.ca>*{max-width:1220px;margin-left:auto;margin-right:auto}.ca header>a{display:inline-flex;align-items:center;gap:7px;color:#111;text-decoration:none;font-weight:900}.ca header>span{display:block;margin-top:22px;color:#0783c9;font-size:10px;font-weight:950;letter-spacing:.16em}.ca h1,.ca h2{font-family:'Bebas Neue',Impact,sans-serif;text-transform:uppercase}.ca h1{font-size:clamp(4rem,9vw,7rem);line-height:.84;margin:8px 0}.ca header p{color:#687385}.ca-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:11px;margin-top:20px}.ca-card,.ca-panels>article,.ca-state{background:#fff;border:1px solid #dce4ea;border-radius:16px;box-shadow:0 8px 22px rgba(15,23,42,.05)}.ca-card{display:grid;grid-template-columns:auto 1fr;align-items:center;gap:8px;padding:18px}.ca-card svg{grid-row:1/3;color:#0783c9}.ca-card span{color:#687385;font-size:10px;font-weight:950;text-transform:uppercase}.ca-card strong{font-size:31px}.ca-panels{display:grid;grid-template-columns:2fr 1fr;gap:12px;margin-top:12px}.ca-panels>article{padding:19px}.ca-panels h2{font-size:31px;margin:0 0 15px}.ca-chart{height:230px;display:flex;align-items:end;gap:5px;border-bottom:1px solid #dce4ea}.ca-chart>div{height:100%;flex:1;display:flex;flex-direction:column;justify-content:end;align-items:center;gap:5px}.ca-chart i{display:block;width:100%;max-width:20px;border-radius:5px 5px 0 0;background:#35b6ff;min-height:6px}.ca-chart span{font-size:8px;color:#7d8791}.ca-device{display:flex;justify-content:space-between;padding:12px 0;border-top:1px solid #edf0f2;text-transform:capitalize}.ca-panels small{display:block;margin-top:18px;color:#7b8590;line-height:1.45}.ca-state{margin-top:20px;padding:40px;text-align:center}.ca-state.error{color:#b01820}@media(max-width:760px){.ca-grid{grid-template-columns:1fr 1fr}.ca-panels{grid-template-columns:1fr}.ca-card strong{font-size:26px}}`

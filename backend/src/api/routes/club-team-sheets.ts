import { Router } from 'express'
import { prisma } from '../../db/client.js'
import { authenticateClubUser, auditMembership, requireActiveClubMembership, roleCan } from '../../auth/club-auth.js'
import { upsertCanonicalGoalKicker } from '../../services/canonical-goal-kicker-upsert.js'

const POSITIONS = ['BP_LEFT','FB','BP_RIGHT','HBF_LEFT','CHB','HBF_RIGHT','WING_LEFT','CENTRE','WING_RIGHT','HFF_LEFT','CHF','HFF_RIGHT','FP_LEFT','FF','FP_RIGHT','RUCK','RUCK_ROVER','ROVER','INTERCHANGE_1','INTERCHANGE_2','INTERCHANGE_3','INTERCHANGE_4','EMERGENCY_1','EMERGENCY_2','EMERGENCY_3'] as const
const STARTING_POSITIONS = POSITIONS.slice(0,18)
const router = Router()

let tablesReady: Promise<void> | null = null
function ensureTables() {
  if (!tablesReady) tablesReady = (async () => {
    await prisma.$executeRawUnsafe(`CREATE TABLE IF NOT EXISTS football_club_players (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),club_id text NOT NULL,player_id text NULL,player_name text NOT NULL,jumper_number integer NULL,preferred_position text NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())`)
    await prisma.$executeRawUnsafe(`CREATE UNIQUE INDEX IF NOT EXISTS football_club_players_identity ON football_club_players (club_id, lower(player_name))`)
    await prisma.$executeRawUnsafe(`CREATE TABLE IF NOT EXISTS football_team_sheets (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),club_id text NOT NULL,league_id text NULL,season text NOT NULL,grade text NOT NULL DEFAULT 'Senior Football',round_label text NOT NULL,opponent_name text NULL,match_date date NULL,status text NOT NULL DEFAULT 'DRAFT',published_at timestamptz NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now())`)
    await prisma.$executeRawUnsafe(`CREATE UNIQUE INDEX IF NOT EXISTS football_team_sheets_identity ON football_team_sheets (club_id, season, grade, round_label)`)
    await prisma.$executeRawUnsafe(`CREATE TABLE IF NOT EXISTS football_team_sheet_players (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),team_sheet_id uuid NOT NULL REFERENCES football_team_sheets(id) ON DELETE CASCADE,club_player_id uuid NOT NULL REFERENCES football_club_players(id) ON DELETE CASCADE,position_code text NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(team_sheet_id, club_player_id),UNIQUE(team_sheet_id, position_code))`)
  })().catch(error => { tablesReady = null; throw error })
  return tablesReady
}

function normaliseRound(value: unknown) { const raw=String(value??'').trim();if(!raw)return'';if(!/^round\b/i.test(raw))return raw;const suffix=raw.replace(/^(?:round\s+)+/i,'').trim();return suffix?`Round ${suffix}`:'Round' }
function cleanName(value:unknown){return String(value??'').trim().replace(/\s+/g,' ').slice(0,120)}
function validDate(value:unknown){if(!value)return null;const raw=String(value).slice(0,10);return /^\d{4}-\d{2}-\d{2}$/.test(raw)&&!Number.isNaN(new Date(`${raw}T00:00:00Z`).getTime())?raw:null}

type Membership = { id:string; role: Parameters<typeof roleCan>[0] }
type ClubPlayer = { id:string; playerId:string|null; playerName:string; jumperNumber:number|null; preferredPosition:string|null; active:boolean }
type Sheet = { id:string; clubId:string; clubName:string|null; leagueId:string|null; leagueName:string|null; season:string; grade:string; roundLabel:string; opponentName:string|null; matchDate:string|null; status:string; publishedAt:string|null }
function allowTeamSelection(req:any,res:any,next:any){const membership=res.locals.clubMembership as Membership|undefined;if(!membership||!roleCan(membership.role,'team_selection'))return res.status(403).json({error:'Your club role cannot manage team selection'});next()}
async function loadSheet(sheetId:string):Promise<(Sheet&{players:Array<ClubPlayer&{clubPlayerId:string;positionCode:string}>})|null>{const rows=await prisma.$queryRawUnsafe<Sheet[]>(`SELECT s.id::text AS id,s.club_id AS "clubId",c.name AS "clubName",s.league_id AS "leagueId",l.name AS "leagueName",s.season,s.grade,s.round_label AS "roundLabel",s.opponent_name AS "opponentName",s.match_date AS "matchDate",s.status,s.published_at AS "publishedAt" FROM football_team_sheets s LEFT JOIN clubs c ON c.id::text=s.club_id LEFT JOIN leagues l ON l.id::text=s.league_id WHERE s.id::text=$1`,sheetId);const sheet=rows[0];if(!sheet)return null;const players=await prisma.$queryRawUnsafe<Array<ClubPlayer&{clubPlayerId:string;positionCode:string}>>(`SELECT cp.id::text AS "clubPlayerId",cp.id::text AS id,cp.player_id AS "playerId",cp.player_name AS "playerName",cp.jumper_number AS "jumperNumber",cp.preferred_position AS "preferredPosition",cp.active,tsp.position_code AS "positionCode" FROM football_team_sheet_players tsp JOIN football_club_players cp ON cp.id=tsp.club_player_id WHERE tsp.team_sheet_id::text=$1 ORDER BY tsp.position_code`,sheetId);return{...sheet,roundLabel:normaliseRound(sheet.roundLabel),players}}
async function sheetForClub(sheetId:string,clubId:string){const sheet=await loadSheet(sheetId);return sheet?.clubId===clubId?sheet:null}
async function ensurePlayerProfile(player:ClubPlayer,sheet:Sheet){if(player.playerId)return player.playerId;if(!sheet.clubName||!sheet.leagueId||!sheet.leagueName)return null;const result=await upsertCanonicalGoalKicker({playerName:player.playerName,clubId:sheet.clubId,clubName:sheet.clubName,leagueId:sheet.leagueId,leagueName:sheet.leagueName,season:sheet.season,grade:sheet.grade,goals:0,matches:null,sourceUrl:null,sourceType:'MANUAL_ENTRY'});await prisma.$executeRawUnsafe(`UPDATE football_club_players SET player_id=$2,updated_at=now() WHERE id::text=$1`,player.id,result.playerRowId);return result.playerRowId}
async function audit(req:any,res:any,action:string,detail:unknown){const membership=res.locals.clubMembership as Membership;await auditMembership(membership.id,req.params.clubId,req.clubUser!.id,action,req.clubUser!.id,detail)}

router.use(authenticateClubUser)
router.use('/clubs/:clubId',requireActiveClubMembership,allowTeamSelection)
router.get('/clubs/:clubId/players',async(req,res)=>{try{await ensureTables();const rows=await prisma.$queryRawUnsafe(`SELECT id::text AS id,player_id AS "playerId",player_name AS "playerName",jumper_number AS "jumperNumber",preferred_position AS "preferredPosition",active FROM football_club_players WHERE club_id=$1 ORDER BY active DESC,player_name`,req.params.clubId);res.json({data:rows,positions:POSITIONS})}catch(error){res.status(500).json({error:'Unable to load club squad',detail:String(error)})}})
router.post('/clubs/:clubId/players',async(req,res)=>{try{await ensureTables();const playerName=cleanName(req.body?.playerName);if(playerName.length<2)return res.status(400).json({error:'Enter a valid player name'});const jumper=req.body?.jumperNumber===''||req.body?.jumperNumber==null?null:Number(req.body.jumperNumber);if(jumper!=null&&(!Number.isInteger(jumper)||jumper<0||jumper>999))return res.status(400).json({error:'Jumper number must be between 0 and 999'});const preferred=String(req.body?.preferredPosition??'').trim().toUpperCase();if(preferred&&!POSITIONS.includes(preferred as any))return res.status(400).json({error:'Invalid preferred position'});const rows=await prisma.$queryRawUnsafe<ClubPlayer[]>(`INSERT INTO football_club_players (club_id,player_id,player_name,jumper_number,preferred_position) VALUES ($1,$2,$3,$4,$5) ON CONFLICT (club_id,lower(player_name)) DO UPDATE SET jumper_number=EXCLUDED.jumper_number,preferred_position=EXCLUDED.preferred_position,active=true,updated_at=now() RETURNING id::text AS id,player_id AS "playerId",player_name AS "playerName",jumper_number AS "jumperNumber",preferred_position AS "preferredPosition",active`,req.params.clubId,req.body?.playerId?String(req.body.playerId):null,playerName,jumper,preferred||null);await audit(req,res,'SQUAD_PLAYER_SAVED',{playerId:rows[0].id,playerName,jumperNumber:jumper});res.status(201).json({data:rows[0]})}catch(error){res.status(500).json({error:'Unable to save squad player',detail:String(error)})}})
router.patch('/clubs/:clubId/players/:playerId',async(req,res)=>{try{await ensureTables();const rows=await prisma.$queryRawUnsafe<ClubPlayer[]>(`SELECT id::text AS id,player_id AS "playerId",player_name AS "playerName",jumper_number AS "jumperNumber",preferred_position AS "preferredPosition",active FROM football_club_players WHERE id::text=$1 AND club_id=$2 LIMIT 1`,req.params.playerId,req.params.clubId);const before=rows[0];if(!before)return res.status(404).json({error:'Squad player not found'});const playerName=req.body?.playerName===undefined?before.playerName:cleanName(req.body.playerName);if(playerName.length<2)return res.status(400).json({error:'Enter a valid player name'});const jumper=req.body?.jumperNumber===undefined?before.jumperNumber:req.body.jumperNumber===''||req.body.jumperNumber==null?null:Number(req.body.jumperNumber);if(jumper!=null&&(!Number.isInteger(jumper)||jumper<0||jumper>999))return res.status(400).json({error:'Jumper number must be between 0 and 999'});const active=req.body?.active===undefined?before.active:Boolean(req.body.active);await prisma.$executeRawUnsafe(`UPDATE football_club_players SET player_name=$3,jumper_number=$4,active=$5,updated_at=now() WHERE id::text=$1 AND club_id=$2`,req.params.playerId,req.params.clubId,playerName,jumper,active);await audit(req,res,'SQUAD_PLAYER_UPDATED',{before:{playerName:before.playerName,jumperNumber:before.jumperNumber,active:before.active},after:{playerName,jumperNumber:jumper,active}});res.json({data:{...before,playerName,jumperNumber:jumper,active}})}catch(error){res.status(500).json({error:'Unable to update squad player',detail:String(error)})}})
router.get('/clubs/:clubId/sheets',async(req,res)=>{try{await ensureTables();const rows=await prisma.$queryRawUnsafe<Array<{id:string}>>(`SELECT id::text AS id FROM football_team_sheets WHERE club_id=$1 ORDER BY match_date DESC NULLS LAST,created_at DESC`,req.params.clubId);const data=await Promise.all(rows.map(row=>loadSheet(row.id)));res.json({data:data.filter(Boolean),positions:POSITIONS})}catch(error){res.status(500).json({error:'Unable to load team sheets',detail:String(error)})}})
router.post('/clubs/:clubId/sheets',async(req,res)=>{try{await ensureTables();const season=String(req.body?.season??new Date().getFullYear()).trim().slice(0,20),grade=String(req.body?.grade??'Senior Football').trim().slice(0,120),roundLabel=normaliseRound(req.body?.roundLabel).slice(0,120),opponentName=cleanName(req.body?.opponentName)||null,matchDate=validDate(req.body?.matchDate);if(!roundLabel)return res.status(400).json({error:'Round is required'});if(req.body?.matchDate&&!matchDate)return res.status(400).json({error:'Enter a valid match date'});const rows=await prisma.$queryRawUnsafe<Array<{id:string}>>(`INSERT INTO football_team_sheets (club_id,league_id,season,grade,round_label,opponent_name,match_date) VALUES ($1,$2,$3,$4,$5,$6,$7::date) ON CONFLICT (club_id,season,grade,round_label) DO UPDATE SET league_id=EXCLUDED.league_id,opponent_name=EXCLUDED.opponent_name,match_date=EXCLUDED.match_date,updated_at=now() RETURNING id::text AS id`,req.params.clubId,req.body?.leagueId?String(req.body.leagueId):null,season,grade,roundLabel,opponentName,matchDate);await audit(req,res,'TEAM_SHEET_OPENED',{sheetId:rows[0].id,season,grade,roundLabel,opponentName,matchDate});res.status(201).json({data:await loadSheet(rows[0].id)})}catch(error){res.status(500).json({error:'Unable to create team sheet',detail:String(error)})}})
router.put('/clubs/:clubId/sheets/:sheetId/positions',async(req,res)=>{try{await ensureTables();const sheet=await sheetForClub(req.params.sheetId,req.params.clubId);if(!sheet)return res.status(404).json({error:'Team sheet not found for this club'});if(sheet.status==='PUBLISHED')return res.status(409).json({error:'Return the team to draft before changing positions'});const incoming=Array.isArray(req.body?.positions)?req.body.positions:[];const clean=incoming.map((row:any)=>({positionCode:String(row.positionCode??''),clubPlayerId:String(row.clubPlayerId??'')})).filter((row:any)=>POSITIONS.includes(row.positionCode as any)&&row.clubPlayerId);if(new Set(clean.map((r:any)=>r.positionCode)).size!==clean.length||new Set(clean.map((r:any)=>r.clubPlayerId)).size!==clean.length)return res.status(400).json({error:'Each player and position can only be used once'});const players=clean.length?await prisma.$queryRawUnsafe<ClubPlayer[]>(`SELECT id::text AS id,player_id AS "playerId",player_name AS "playerName",jumper_number AS "jumperNumber",preferred_position AS "preferredPosition",active FROM football_club_players WHERE club_id=$1 AND active=true AND id=ANY($2::uuid[])`,req.params.clubId,clean.map((r:any)=>r.clubPlayerId)):[];if(players.length!==clean.length)return res.status(400).json({error:'One or more selected players are not active in this club squad'});for(const player of players)await ensurePlayerProfile(player,sheet);await prisma.$transaction(async tx=>{await tx.$executeRawUnsafe(`DELETE FROM football_team_sheet_players WHERE team_sheet_id::text=$1`,req.params.sheetId);for(const row of clean)await tx.$executeRawUnsafe(`INSERT INTO football_team_sheet_players (team_sheet_id,club_player_id,position_code) VALUES ($1::uuid,$2::uuid,$3)`,req.params.sheetId,row.clubPlayerId,row.positionCode)});await audit(req,res,'TEAM_POSITIONS_SAVED',{sheetId:req.params.sheetId,playerCount:clean.length,positions:clean});res.json({data:await loadSheet(req.params.sheetId),profilesLinked:players.length})}catch(error){res.status(500).json({error:'Unable to save team positions',detail:String(error)})}})
router.put('/clubs/:clubId/sheets/:sheetId',async(req,res)=>{try{await ensureTables();const sheet=await sheetForClub(req.params.sheetId,req.params.clubId);if(!sheet)return res.status(404).json({error:'Team sheet not found for this club'});const status=String(req.body?.status??'DRAFT').toUpperCase()==='PUBLISHED'?'PUBLISHED':'DRAFT';const matchDate=req.body?.matchDate?validDate(req.body.matchDate):null;if(req.body?.matchDate&&!matchDate)return res.status(400).json({error:'Enter a valid match date'});if(status==='PUBLISHED'){const rows=await prisma.$queryRawUnsafe<Array<{positionCode:string}>>(`SELECT position_code AS "positionCode" FROM football_team_sheet_players WHERE team_sheet_id=$1::uuid`,req.params.sheetId);const present=new Set(rows.map(row=>row.positionCode));const missing=STARTING_POSITIONS.filter(position=>!present.has(position));if(missing.length)return res.status(400).json({error:`Complete all 18 starting positions before publishing (${missing.length} remaining)`});if(!sheet.opponentName&&!req.body?.opponentName)return res.status(400).json({error:'Opponent is required before publishing'});if(!sheet.matchDate&&!matchDate)return res.status(400).json({error:'Match date is required before publishing'})}const opponentName=req.body?.opponentName===undefined?null:cleanName(req.body.opponentName)||null;await prisma.$executeRawUnsafe(`UPDATE football_team_sheets SET status=$2,published_at=CASE WHEN $2='PUBLISHED' THEN COALESCE(published_at,now()) ELSE NULL END,opponent_name=COALESCE($3,opponent_name),match_date=COALESCE($4::date,match_date),updated_at=now() WHERE id::text=$1`,req.params.sheetId,status,opponentName,matchDate);const updated=await loadSheet(req.params.sheetId);await audit(req,res,status==='PUBLISHED'?'TEAM_PUBLISHED':'TEAM_RETURNED_TO_DRAFT',{sheetId:req.params.sheetId,roundLabel:updated?.roundLabel,opponentName:updated?.opponentName,matchDate:updated?.matchDate,playerCount:updated?.players.length??0});if(status==='PUBLISHED'&&updated){const dedupeKey=`team-sheet:${updated.id}`;await prisma.notification.upsert({where:{dedupeKey},create:{recipientScope:'PLATFORM',type:'TEAM_SELECTION',category:'FEED',severity:'INFO',title:`${updated.clubName??'Club'} team named`,body:`${updated.roundLabel}${updated.opponentName?` v ${updated.opponentName}`:''}`,entityType:'CLUB',entityId:req.params.clubId,data:JSON.stringify({href:`/team/${encodeURIComponent(req.params.clubId)}?tab=team-selection`,clubId:req.params.clubId,teamSheetId:updated.id,shareCardType:'TEAM_SELECTION'}),status:'DELIVERED',dedupeKey},update:{title:`${updated.clubName??'Club'} team named`,body:`${updated.roundLabel}${updated.opponentName?` v ${updated.opponentName}`:''}`,data:JSON.stringify({href:`/team/${encodeURIComponent(req.params.clubId)}?tab=team-selection`,clubId:req.params.clubId,teamSheetId:updated.id,shareCardType:'TEAM_SELECTION'}),status:'DELIVERED'}})}res.json({data:updated})}catch(error){res.status(500).json({error:'Unable to update team sheet',detail:String(error)})}})
router.delete('/clubs/:clubId/sheets/:sheetId',async(req,res)=>{try{await ensureTables();const sheet=await sheetForClub(req.params.sheetId,req.params.clubId);if(!sheet)return res.status(404).json({error:'Team sheet not found for this club'});if(sheet.status==='PUBLISHED')return res.status(409).json({error:'Return the team to draft before deleting it'});await prisma.$executeRawUnsafe(`DELETE FROM football_team_sheets WHERE id::text=$1 AND club_id=$2`,req.params.sheetId,req.params.clubId);await audit(req,res,'TEAM_SHEET_DELETED',{sheetId:sheet.id,roundLabel:sheet.roundLabel,opponentName:sheet.opponentName});res.json({data:{deleted:true}})}catch(error){res.status(500).json({error:'Unable to delete team sheet',detail:String(error)})}})

export { router as clubTeamSheetsRouter }

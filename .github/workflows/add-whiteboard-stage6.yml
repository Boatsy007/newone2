name: Add Whiteboard Stage 6
on:
  push:
    branches: [newone1]
    paths:
      - scripts/add_whiteboard_stage6.py
      - backend/src/api/routes/whiteboard-live.ts
      - backend/api/index.ts
      - .github/workflows/add-whiteboard-stage6.yml
permissions:
  contents: write
jobs:
  implement:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: newone1
          fetch-depth: 0
      - name: Apply Stage 6 UI
        run: python scripts/add_whiteboard_stage6.py
      - name: Install frontend
        run: npm ci
      - name: Install backend
        run: npm --prefix backend ci
      - name: Build production app
        run: npm run build
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/CoachAppWhiteboardStage2.tsx src/pages/CoachAppTacticAssignment.tsx
          if git diff --cached --quiet; then echo "No changes"; exit 0; fi
          git commit -m "Add live collaboration and video to whiteboard"
          git push origin HEAD:newone1

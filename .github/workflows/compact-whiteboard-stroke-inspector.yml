name: Compact Whiteboard Stroke Inspector
on:
  push:
    branches: [newone1]
    paths:
      - '.github/scripts/compact_whiteboard_stroke_inspector.py'
      - '.github/workflows/compact-whiteboard-stroke-inspector.yml'
permissions:
  contents: write
jobs:
  implement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: newone1
          fetch-depth: 0
      - name: Apply compact stroke inspector
        run: python .github/scripts/compact_whiteboard_stroke_inspector.py
      - name: Install frontend
        run: npm ci
      - name: Install backend
        run: npm ci
        working-directory: backend
      - name: Build production app
        run: npm run build
      - name: Commit implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/pages/CoachAppWhiteboardStage2.tsx
          if git diff --cached --quiet; then
            echo 'No changes to commit'
          else
            git commit -m 'Move drawing adjustment bar above drawing tools'
            git push origin newone1
          fi

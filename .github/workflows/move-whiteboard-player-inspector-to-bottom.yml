name: Move Whiteboard Player Inspector To Bottom
on:
  push:
    branches: [newone1]
    paths:
      - '.github/scripts/move_whiteboard_player_inspector_to_bottom.py'
      - '.github/workflows/move-whiteboard-player-inspector-to-bottom.yml'
permissions:
  contents: write
jobs:
  implement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: newone1
          fetch-depth: 0
      - name: Apply bottom player inspector
        run: python .github/scripts/move_whiteboard_player_inspector_to_bottom.py
      - name: Install frontend
        run: npm ci
      - name: Install backend
        run: npm ci
        working-directory: backend
      - name: Build production app
        run: npm run build
      - name: Commit implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/pages/CoachAppWhiteboardStage2.tsx
          if git diff --cached --quiet; then
            echo 'No changes to commit'
          else
            git commit -m 'Move selected player controls into bottom app bar'
            git push origin newone1
          fi

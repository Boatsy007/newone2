name: Add Match Day Rotation Mode
on:
  push:
    branches: [newone1]
    paths:
      - scripts/add_matchday_rotation_mode.py
      - .github/workflows/add-matchday-rotation-mode.yml
permissions:
  contents: write
jobs:
  implement:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: newone1
          fetch-depth: 0
      - name: Apply rotation mode
        run: python scripts/add_matchday_rotation_mode.py
      - name: Install frontend
        run: npm ci
      - name: Install backend
        run: npm --prefix backend ci
      - name: Build frontend
        run: npm run build
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/CoachAppMatchDay.tsx
          if git diff --cached --quiet; then echo "No changes"; exit 0; fi
          git commit -m "Add multi-player Match Day rotation mode"
          git push origin HEAD:newone1
